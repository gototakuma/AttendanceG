<h1>勤怠修正ログ</h1>
<table class="table-bordered table-striped table-condensed table-hover">
  <thead>
    <tr>
      <td>日付</td>
      <td>変更前出社時間</td>
      <td>変更前退社時間</td>
      <td>変更後出社時間</td>
      <td>変更後退社時間</td>
      <td>承認者</td>
    </tr>
  </thead>
  <tbody>
    <% @dates.each do |day| %>
      <tr>
        <% if day.started_be.present? && day.approval_at == "承認" %>
        <td><%= day.worked_on.to_s(:date)%></td>
        <td><%= day.started_at.to_s(:hourmin)  if day.started_at.present?%></td>
        <td><%= day.finished_at.to_s(:hourmin) if day.finished_at.present?%></td>
        <td><%= day.started_be.to_s(:hourmin)  if day.started_be.present?%></td>
        <td><%= day.finished_be.to_s(:hourmin) if day.finished_be.present?%></td>
        <td><%= "上長#{day.instructor_nameat-1}" if day.started_be.present?%></td>
        <%end%>
      </tr>
    <%end%>
  </tbody>
</table>